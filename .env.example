# Copy to .env and fill in real values. Never commit .env.

# Claude (scoring)
ANTHROPIC_API_KEY=sk-ant-...

# Gemini (vision)
GOOGLE_API_KEY=AIza...

# Optional model overrides
# SCORING_MODEL=claude-sonnet-4-6
# VISION_MODEL=gemini-2.5-flash

# Cloudflare R2 (used by scripts/upload_videos_to_r2.py + the dashboard's
# signed-URL helper on Streamlit Cloud). Account id + bucket are safe to
# share; the access key + secret are NOT — they grant write access.
R2_ACCOUNT_ID=00ee49aaff555c25f705fd3f212f75b0
R2_BUCKET=openhouse-tqm-videos
R2_ACCESS_KEY_ID=...
R2_SECRET_ACCESS_KEY=...
